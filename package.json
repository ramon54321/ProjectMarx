{
  "scripts": {
    "build": "rm -rf dist && rm -rf js && tsc && webpack",
    "deploy": "./deploy.sh"
  },
  "devDependencies": {
    "chart.js": "^2.7.3",
    "ramda": "^0.26.1",
    "webpack": "^4.28.4",
    "webpack-cli": "^3.2.1"
  }
}
